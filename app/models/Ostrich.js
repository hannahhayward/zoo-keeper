export class Ostrich {
  constructor(
    name = '',
    height = '',
    fast = true,
    alive = true,
  ) {
    this.name = name
    this.height = height
    this.fast = fast
    this.alive = alive

  }
}